The character <%= characterName %> in the narration "<%= narrationTitle %>" was claimed. See the character sheet for reference:

<%= characterSheetLink %>

<% if (numberUnclaimed) { %>
There are still <%= numberUnclaimed %> unclaimed characters in your story, but you can still go to the narration page and create the first chapter if you want.
<% } else { %>
Now all characters in your story are claimed! You can go to the narration page and create the first chapter. Have fun!
<% } %>

<%= narrationLink %>
