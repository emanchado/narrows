<%= senderName %> has sent <%= recipientListString %> a new message in chapter "<%= chapterTitle %>".

The mesage is:

<%= messageText %>

You can see it in context, and reply, at <%= chapterUrl %> .
